import React from "react";
import Dashboard from './Dashboard'
const Profile = (code) => {
    return (
        <div>
        <Dashboard code={code}/>
        </div>
    );
};

export default Profile;
